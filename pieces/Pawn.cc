#include "Pawn.h"

Piecetype Pawn::getType() {
    return Piecetype::Pawn;
}

std::vector<std::pair<int,int>> Pawn::getMoves() {
    std::vector<std::pair<int,int>> moves;

    //get moves for pawn in moves

    return moves;
}